#include <stdio.h>
int main() 
{
    for (int x = 1 ; x <= 12; x = x + 12) 
    {
        for (int y = x; y <= 12; y = y + 12) 
    }  
    }
    for ( int z = x; z = <=12; z = z + 12){
        printf("x%d y%d z%d\n", x,y,z);
    }

      return 0;
}


