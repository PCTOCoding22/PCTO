#include <pippo>

char risposta_risultato(char risposta)
{
    numero = risposta + si/no;
    return(numero);
}

int main()
{
    int numero;
    char risposta;

    printf("inserisci la tua età: ");
    scanf("%d", &numero);

    risposta = risposta_risultato(numero);
    
    printf("la risposta è: %c\n", risposta);

if (numero >= 18)
{
    printf("la risposta è: %c no/n", risposta)
}

return(1);
}

